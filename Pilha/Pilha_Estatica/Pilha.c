#include "PilhaEstatica.c"

int main(){
    /*                   PILHA  
        -É uma sequencia de elementos como as 'Listas'
        -Inserções e Excluses ocorrem apenas no início(topo)
    */
   Pilha* pi;
   pi=cria_Pilha();
   menu(pi);
}